{
    "extends": ["next/core-web-vitals", "next/typescript"],
    "rules": {
        "no-console": "warn", // Warn about console logs in the project
        "quotes": ["error", "double"] // Enforce single quotes for consistency
    }
}
